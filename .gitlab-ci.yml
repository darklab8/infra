variables:
  IMAGE_PIPELINE_RUNNER: docker:20.10.21-cli
  DOCKER_HOST: tcp://dind-service:2375

stages:
  - test

check-ci-runner:
  image: ${IMAGE_PIPELINE_RUNNER}
  stage: test
  script:
    - echo "checking CI runner"
    - docker ps

# TODO write manual action to deploy your runners for darklab/shapevpn/work